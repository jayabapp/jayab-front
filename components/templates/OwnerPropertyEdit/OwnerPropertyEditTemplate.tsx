import type { OwnerPropertyTemplateProps } from "@/types/components/templates/owner-property";

const OwnerPropertyEditTemplate = ({ children }: OwnerPropertyTemplateProps) => (
  <main className="profile-container w-full">{children}</main>
);

export default OwnerPropertyEditTemplate;
