import type { OwnerPropertyTemplateProps } from "@/types/components/templates/owner-property";

const OwnerPropertiesTemplate = ({ children }: OwnerPropertyTemplateProps) => (
  <main className="profile-container">{children}</main>
);

export default OwnerPropertiesTemplate;
