import type { AdvisorTemplateProps } from "@/types/components/templates/advisors";

const AdvisorRegistrationTemplate = ({ children }: AdvisorTemplateProps) => (
  <main className="profile-container w-full">{children}</main>
);

export default AdvisorRegistrationTemplate;
